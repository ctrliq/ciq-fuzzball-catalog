# Copyright 2025 CIQ, Inc. All rights reserved.
id: "blast_application" # needs to be **unique** per application, changing results in a new application
name: "BLAST"
category: "OTHER"
description: "The BLAST example job provides a basic skeleton Fuzzball workflow to retrieve query and database sequences using efetch, assembles the database, and runs the query against the assembled datbase using BLAST."
