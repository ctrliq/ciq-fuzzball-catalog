# Copyright 2026 CIQ, Inc. All rights reserved.

{{- $model := trim .Model }}
{{- if not (hasPrefix "hf://" $model) }}
  {{- fail "Model must be an hf:// URI, e.g. hf://openai/gpt-oss-20b" }}
{{- end }}
{{- /* The URI may carry query parameters (a revision, or include/exclude
       filters that keep the download to the files the server actually reads).
       They belong in the ingress source, but not in the served model name. */}}
{{- $model_ref := trim (trimPrefix "hf://" $model) }}
{{- $model_name := trim (index (splitList "?" $model_ref) 0) }}
{{- /* A pinned revision decides which weights the download produces, so it has
       to key the directory as well: without it two revisions of one repository
       would share a directory on a persistent volume. Filters only decide which
       files of that revision are fetched, so they are deliberately not part of
       the key — narrowing a filter should not orphan an existing download. */}}
{{- $revision := "" }}
{{- if gt (len (splitList "?" $model_ref)) 1 }}
  {{- range (splitList "&" (index (splitList "?" $model_ref) 1)) }}
    {{- if hasPrefix "revision=" . }}
      {{- $revision = trimPrefix "revision=" . }}
    {{- end }}
  {{- end }}
{{- end }}
{{- if not $model_name }}
  {{- fail "Model must name a repository, e.g. hf://openai/gpt-oss-20b" }}
{{- end }}
{{- /* The repository name is interpolated into a filesystem path, a shell
       string and a Python string literal, so restrict it to the characters a
       HuggingFace repository id can actually contain. */}}
{{- if not (regexMatch "^[A-Za-z0-9._/-]+$" $model_name) }}
  {{- fail "Model repository may contain only letters, digits, and . _ / -" }}
{{- end }}
{{- if contains ".." $model_name }}
  {{- fail "Model repository must not contain '..'" }}
{{- end }}
{{- $model_dir := $model_name | lower | replace "/" "--" }}
{{- if $revision }}
  {{- if not (regexMatch "[A-Za-z0-9]" $revision) }}
    {{- fail "Model revision must contain at least one letter or digit" }}
  {{- end }}
  {{- $model_dir = printf "%s@%s" $model_dir (trunc 40 (regexReplaceAll "[^a-z0-9._-]" (lower $revision) "-")) }}
{{- end }}

{{- $gpu := lower (trim .Gpu) }}
{{- if and (ne $gpu "nvidia") (ne $gpu "amd") }}
  {{- fail "Gpu must be one of: nvidia, amd" }}
{{- end }}
{{- $gpu_device := "nvidia.com/gpu" }}
{{- if eq $gpu "amd" }}
  {{- $gpu_device = "amd.com/gpu" }}
{{- end }}

{{- $scope := lower (trim .Scope) }}
{{- if not (has $scope (list "user" "group" "organization" "public")) }}
  {{- fail "Scope must be one of: user, group, organization, public" }}
{{- end }}

{{- $volume := trim .Volume }}
{{- if not $volume }}
  {{- fail "Volume must be 'ephemeral' or the name of a persistent volume" }}
{{- end }}
{{- /* Only structural integrity: name validity is the server's job, but a quote
       or backslash would corrupt the rendered YAML before the server sees it. */}}
{{- if or (contains "\"" $volume) (contains "\\" $volume) }}
  {{- fail "Volume must not contain quotes or backslashes" }}
{{- end }}

{{- $min_replicas := int .MinReplicas }}
{{- $max_replicas := int .MaxReplicas }}
{{- $drain_period := int .DrainPeriod }}
{{- $scale_up_rpm := int .ScaleUpRequestsPerMinute }}
{{- if lt $scale_up_rpm 1 }}
  {{- fail "ScaleUpRequestsPerMinute must be at least 1" }}
{{- end }}
{{- $scale_up_cooldown := int .ScaleUpCooldown }}
{{- if lt $scale_up_cooldown 15 }}
  {{- fail "ScaleUpCooldown must be at least 15 seconds" }}
{{- end }}
{{- $gpus_per_replica := int .GpusPerReplica }}
{{- $tensor_parallel := max 1 $gpus_per_replica }}
{{- /* Values files written before this knob existed -- the per-model preset
       entries among them -- carry no Nodes at all, and must keep rendering as
       single-node. Those arrive as an invalid kind, which is what separates
       them from an explicit 0: 'default' cannot, because it treats a zero
       uint as empty and would silently read Nodes=0 as Nodes=1. */}}
{{- $nodes := 1 }}
{{- if not (kindIs "invalid" .Nodes) }}
  {{- $nodes = int .Nodes }}
{{- end }}
{{- if lt $nodes 1 }}
  {{- fail "Nodes must be at least 1; omit it to serve each replica on a single node" }}
{{- end }}
{{- /* A replica's data-parallel world spans every GPU of every node in its
       group, so that product -- not the per-node GPU count -- is what decides
       whether expert parallelism has anything to shard across. */}}
{{- $dp_size := mul $nodes $gpus_per_replica }}
{{- $ep := lower (trim (default "auto" .ExpertParallelism)) }}
{{- if not (has $ep (list "auto" "true" "false")) }}
  {{- fail "ExpertParallelism must be one of: auto, true, false" }}
{{- end }}
{{- /* vLLM turns expert parallelism off when the data-parallel size is 1,
       because a single GPU has nothing to shard the experts across. Refuse the
       combination rather than reporting a layout the server will not use. */}}
{{- if and (eq $ep "true") (lt $dp_size 2) }}
  {{- fail "ExpertParallelism=true requires Nodes x GpusPerReplica >= 2; expert parallelism has no effect on a single GPU" }}
{{- end }}
{{- /* A multi-node group exists to serve one model across nodes with its
       experts sharded over the group. Data-parallel replication of a dense
       model is what the replica pool itself already does, so a group with
       expert parallelism switched off would buy nothing for twice the
       hardware. */}}
{{- if and (gt $nodes 1) (eq $ep "false") }}
  {{- fail "Nodes > 1 requires expert parallelism; set ExpertParallelism=auto or true, or serve a dense model with Nodes=1 and more replicas" }}
{{- end }}
{{- if and (gt $nodes 1) (lt $gpus_per_replica 1) }}
  {{- fail "Nodes > 1 requires GpusPerReplica >= 1; every node of a group serves part of the model" }}
{{- end }}
{{- /* Fixed port for data-parallel RPC between a group's nodes. Multinode
       services run on the host network, so this must be free on every node of
       the group and reachable between them; it is not a served port and needs
       no endpoint. */}}
{{- $dp_rpc_port := 13345 }}
{{- $all2all := trim (default "" .All2AllBackend) }}
{{- if and $all2all (not (has $all2all (list "allgather_reducescatter" "deepep_high_throughput" "deepep_low_latency"))) }}
  {{- fail "All2AllBackend must be empty, allgather_reducescatter, deepep_high_throughput, or deepep_low_latency" }}
{{- end }}
{{- $gpu_model := trim .GpuModel }}
{{- $extra_args := trim .ExtraArgs }}
{{- if and $extra_args (not (regexMatch "^[A-Za-z0-9 ._=/-]+$" $extra_args)) }}
  {{- fail "ExtraArgs may contain only letters, digits, spaces, and . _ = / -" }}
{{- end }}
{{- /* Environment variables for the server process, on every rank of a group.
       Kept separate from ExtraArgs because the settings that matter here --
       collective-library tuning above all -- are read from the environment and
       have no command-line equivalent. Each entry is validated as NAME=VALUE so
       nothing can break out into the rendered YAML. */}}
{{- $extra_env := trim (default "" .ExtraEnv) }}
{{- $env_pairs := list }}
{{- range (splitList " " $extra_env) }}
  {{- $pair := trim . }}
  {{- if $pair }}
    {{- if not (regexMatch "^[A-Za-z_][A-Za-z0-9_]*=[A-Za-z0-9_.,:/-]*$" $pair) }}
      {{- fail (printf "ExtraEnv entry %q must be NAME=VALUE, where NAME is a shell identifier and VALUE contains only letters, digits, and . _ , : / -" $pair) }}
    {{- end }}
    {{- $env_pairs = append $env_pairs $pair }}
  {{- end }}
{{- end }}
{{- $hf_token_secret := trim .HfTokenSecret }}

{{- $api_key := trim .ApiKey }}
{{- if .Proxy }}
  {{- if $api_key }}
    {{- if not (regexMatch "^sk-[A-Za-z0-9_-]+$" $api_key) }}
      {{- fail "ApiKey must start with sk- and contain only letters, digits, '-' and '_'" }}
    {{- end }}
  {{- else }}
    {{- $api_key = printf "sk-fb-%d%d%d%d" (randInt 100000000 999999999) (randInt 100000000 999999999) (randInt 100000000 999999999) (randInt 100000000 999999999) }}
  {{- end }}
{{- end }}

{{- $vllm_port := 8000 }}
{{- $litellm_port := randInt 24001 32000 }}
{{- $data_mount := "/data" }}
{{- $model_path := printf "%s/models/%s" $data_mount $model_dir }}

version: v4

volumes:
  data:
    {{- if eq (lower $volume) "ephemeral" }}
    use: ephemeral
    {{- else }}
    use: persistent
    name: "{{ $volume }}"
    {{- end }}
    ingress:
      - source:
          uri: {{ $model }}
          {{- if $hf_token_secret }}
          secret: {{ $hf_token_secret }}
          {{- end }}
        destination:
          uri: file://models/{{ $model_dir }}
        policy:
          timeout:
            execute: {{ .IngressTimeout }}

services:
  vllm:
    persist: true
    image:
      uri: {{ if eq $gpu "amd" }}{{ .VllmRocmImage }}{{ else }}{{ .VllmImage }}{{ end }}
    mounts:
      {{ $data_mount }}:
        volume: data
    env:
      # The model is already local (ingressed); serve fully offline.
      - HF_HUB_OFFLINE=1
      - HF_HOME={{ $data_mount }}/.cache/huggingface
      {{- if eq $gpu "amd" }}
      - PYTORCH_HIP_ALLOC_CONF=expandable_segments:True
      {{- end }}
      {{- range $env_pairs }}
      - {{ . }}
      {{- end }}
    script: |
      #!/bin/sh
      set -u
      # The parallelism layout is decided at runtime: whether the model is
      # MoE is a property of the downloaded snapshot's config.json, which
      # exists only after ingress, never at render time.
      detect=$(python3 - <<'PY'
      import json
      cfg = json.load(open("{{ $model_path }}/config.json"))
      arch = (cfg.get("architectures") or ["unknown"])[0]
      KEYS = ("num_local_experts", "n_routed_experts", "num_experts", "moe_num_experts")
      def moe(c):
          return any(isinstance(c.get(k), int) and c[k] > 1 for k in KEYS)
      # The expert count sits at the top level, or in a nested config block:
      # text_config for multimodal models, ffn_config for DBRX.
      nested = [v for v in cfg.values() if isinstance(v, dict)]
      print(("moe" if moe(cfg) or any(moe(v) for v in nested) else "dense"), arch)
      PY
      ) || { echo "ERROR: cannot read {{ $model_path }}/config.json to detect the model architecture" >&2; exit 1; }
      is_moe=${detect%% *}
      arch=${detect#* }

      use_ep=0
      if [ "{{ $ep }}" = "true" ]; then
        if [ "$is_moe" != "moe" ]; then
          echo "ERROR: ExpertParallelism=true, but model '{{ $model_name }}' (architecture: $arch)" \
               "is not a mixture-of-experts model: config.json has no expert-count field" \
               "(num_local_experts, n_routed_experts, num_experts, moe_num_experts) greater than 1." \
               "Use ExpertParallelism=auto or ExpertParallelism=false." >&2
          exit 1
        fi
        use_ep=1
      elif [ "{{ $ep }}" = "auto" ] && [ "$is_moe" = "moe" ] && [ {{ $dp_size }} -ge 2 ]; then
        use_ep=1
      fi
{{- if gt $nodes 1 }}

      # A group serves one model across {{ $nodes }} nodes, which only pays off
      # with the experts sharded over it, so a dense model here is a
      # misconfiguration rather than something to serve at half the value.
      if [ "$use_ep" -ne 1 ]; then
        echo "ERROR: Nodes={{ $nodes }} needs a mixture-of-experts model, but '{{ $model_name }}'" \
             "(architecture: $arch) is dense. Serve it with Nodes=1 and more replicas instead." >&2
        exit 1
      fi
{{- end }}

      # llm-d wide-EP layout: attention data-parallel across the group's GPUs,
      # experts expert-parallel (EP world size = DP x TP, so TP stays 1).
      if [ "$use_ep" -eq 1 ]; then
        parallel_args="--data-parallel-size {{ $dp_size }} --enable-expert-parallel"
        echo "vllm: $arch ($is_moe), ExpertParallelism={{ $ep }} -> expert parallelism, data-parallel size {{ $dp_size }}{{ if gt $nodes 1 }} across {{ $nodes }} nodes{{ end }}"
      else
        parallel_args="--tensor-parallel-size {{ $tensor_parallel }}"
        echo "vllm: $arch ($is_moe), ExpertParallelism={{ $ep }} -> tensor parallelism, size {{ $tensor_parallel }}"
      fi
{{- if and $all2all (le $nodes 1) }}
      parallel_args="$parallel_args --all2all-backend {{ $all2all }}"
{{- end }}
{{- if gt $nodes 1 }}

      # Flags shared by every rank beyond the parallel layout, passed as one
      # word-split argument so a group's ranks stay byte-identical apart from
      # their own rank offset.
      parallel_extra="{{ if $all2all }}--all2all-backend {{ $all2all }}{{ end }}{{ if $extra_args }}{{ if $all2all }} {{ end }}{{ $extra_args }}{{ end }}"

      # Under implementation: generic the wrapper runs this script on rank 0
      # only; peer ranks wait for work dispatched through the rsh shim, which
      # is what the shipped helper wraps. Both halves of the group are started
      # from here: peers headless, then rank 0 with the API server.
      . /multinode/generic-helper

      # Every rank dials rank 0 for data-parallel RPC, so the coordinator
      # address is this node's IP -- resolved here, on rank 0.
      head_ip="$MULTINODE_NODE_IP"

      # The peer script is single-quoted so $RANK expands on each worker to its
      # own rank; rank-0-side values travel as positional arguments, which the
      # helper quotes for the trip. Each node owns {{ $gpus_per_replica }}
      # consecutive data-parallel ranks, so its offset is rank x that count.
      exec_rankN 'exec vllm serve "$1" \
        --served-model-name "$2" \
        --headless \
        --data-parallel-size "$3" \
        --data-parallel-size-local "$4" \
        --data-parallel-start-rank $(( $RANK * $4 )) \
        --data-parallel-address "$5" \
        --data-parallel-rpc-port "$6" \
        --enable-expert-parallel \
        --tensor-parallel-size 1 \
        --max-model-len "$7" \
        --gpu-memory-utilization "$8" \
        $9' \
        "{{ $model_path }}" "{{ $model_name }}" "{{ $dp_size }}" "{{ $gpus_per_replica }}" \
        "$head_ip" "{{ $dp_rpc_port }}" "{{ .MaxContextSize }}" "{{ .GpuMemoryUtilization }}" \
        "$parallel_extra"

      # Rank 0 owns the endpoint. api-server-count follows vLLM's guidance of
      # one API server per local data-parallel rank.
      exec_rank0 'exec vllm serve "$1" \
        --served-model-name "$2" \
        --data-parallel-size "$3" \
        --data-parallel-size-local "$4" \
        --data-parallel-address "$5" \
        --data-parallel-rpc-port "$6" \
        --api-server-count "$4" \
        --enable-expert-parallel \
        --tensor-parallel-size 1 \
        --max-model-len "$7" \
        --gpu-memory-utilization "$8" \
        --host 0.0.0.0 \
        --port "$9" \
        ${10}' \
        "{{ $model_path }}" "{{ $model_name }}" "{{ $dp_size }}" "{{ $gpus_per_replica }}" \
        "$head_ip" "{{ $dp_rpc_port }}" "{{ .MaxContextSize }}" "{{ .GpuMemoryUtilization }}" \
        "{{ $vllm_port }}" "$parallel_extra"

      wait_rank0
{{- else }}

      exec vllm serve "{{ $model_path }}" \
        --served-model-name "{{ $model_name }}" \
        $parallel_args \
        --max-model-len {{ .MaxContextSize }} \
        --gpu-memory-utilization {{ .GpuMemoryUtilization }} \
        --host 0.0.0.0 \
        --port {{ $vllm_port }}{{ if $extra_args }} {{ $extra_args }}{{ end }}
{{- end }}
{{- if gt $nodes 1 }}
    # Each replica is one gang-scheduled group: Fuzzball starts all {{ $nodes }}
    # nodes together or none, wires their DNS and gossip, and serves the
    # endpoint from rank 0. 'generic' leaves process launch to the workload,
    # which the script does through the shipped helper -- an MPI launcher would
    # otherwise try to start the ranks itself.
    multinode:
      nodes: {{ $nodes }}
      implementation: generic
{{- end }}
    resource:
{{- if gt $nodes 1 }}
      # Per node, not per group: each of the group's {{ $nodes }} nodes gets
      # this shape, so the group's total is {{ $nodes }} times it.
{{- end }}
      cpu:
        cores: {{ .ReplicaCores }}
      memory:
        size: {{ .ReplicaMemory }}
      {{- if gt $gpus_per_replica 0 }}
      devices:
        {{ $gpu_device }}: {{ $gpus_per_replica }}
      {{- if $gpu_model }}
      annotations:
        {{ $gpu_device }}.model: "{{ $gpu_model }}"
      {{- end }}
      {{- end }}
    network:
      ports:
        - name: openai-api
          port: {{ $vllm_port }}
          protocol: tcp
      {{- if not .Proxy }}
      endpoints:
        - name: openai
          port-name: openai-api
          protocol: http
          type: subdomain
          scope: {{ $scope }}
          per-replica: true
          # Discovery contract shared with the litellm gateway entry: it
          # registers endpoints carrying these two annotations.
          annotations:
            ciq.com/api: openai
            ciq.com/model: "{{ $model_name }}"
      {{- end }}
    readiness-probe:
      http-get:
        path: /health
        port: {{ $vllm_port }}
        scheme: HTTP
      initial-delay-seconds: 60
      period-seconds: 15
      timeout-seconds: 5
      failure-threshold: 120
      success-threshold: 1
    autoscaler:
      replicas:
        min: {{ $min_replicas }}
        max: {{ $max_replicas }}
      metrics:
        enabled: true
        path: /metrics
        port: {{ $vllm_port }}
        interval: 15
      {{- if not .Proxy }}
      scale-up:
        triggers:
          # Aggregated because data-parallel serving (expert parallelism)
          # exposes one series per engine rank; a bare vector query would
          # feed the trigger N elements instead of one.
          - metrics-query: sum(vllm:num_requests_waiting) >= bool 2
            cooldown-period: 60
      {{- end }}
      scale-down:
        # Sustained idleness, not an instantaneous sample: a replica between
        # two requests reports 0 running at a scrape instant, which would
        # retire busy replicas every cooldown. max() over engines for the
        # same per-engine-series reason as the scale-up query.
        metrics-query: max(max_over_time(vllm:num_requests_running[5m])) == bool 0
        cooldown-period: 300
        drain-period: {{ $drain_period }}

  {{- if .Proxy }}

  litellm:
    persist: true
    image:
      uri: {{ .LitellmImage }}
    resource:
      cpu:
        cores: 2
      memory:
        size: 4GiB
    network:
      ports:
        - name: openai-api
          port: {{ $litellm_port }}
          protocol: tcp
      endpoints:
        - name: openai
          port-name: openai-api
          protocol: http
          type: subdomain
          scope: {{ $scope }}
    dynamic-config:
      path: /etc/litellm/config.yaml
      services: [vllm]
      script: |
        if [ "${#SERVICES_VLLM_OPENAI_API[@]}" -eq 0 ]; then
          cat <<'EOF'
        model_list: []
        EOF
        else
          cat <<'EOF'
        model_list:
        EOF
          for addr in "${SERVICES_VLLM_OPENAI_API[@]}"; do
            cat <<EOF
          - model_name: "{{ $model_name }}"
            litellm_params:
              model: "hosted_vllm/{{ $model_name }}"
              api_base: "http://${addr}/v1"
        EOF
          done
        fi
        cat <<'EOF'
        general_settings:
          master_key: "{{ $api_key }}"
        litellm_settings:
          callbacks: ["prometheus"]
          num_retries: 2
          # The autoscaler's scraper sends no credentials; without this the
          # scrape gets 401 and no scale trigger can ever fire.
          require_auth_for_metrics_endpoint: false
        EOF
    script: |
      #!/bin/bash
      # LiteLLM reads its config only at startup, while Fuzzball re-renders the
      # dynamic-config file in place as the pool scales; watch the file and
      # restart LiteLLM whenever the backend list changes.
      config=/etc/litellm/config.yaml
      start() {
        litellm --config "$config" --host 0.0.0.0 --port {{ $litellm_port }} &
        litellm_pid=$!
      }
      current() { md5sum "$config" | cut -d' ' -f1; }
      start
      last=$(current)
      while true; do
        sleep 5
        now=$(current)
        if [ "$now" != "$last" ]; then
          # Settle: the file is rewritten in place, so re-read once in case we
          # hashed a partial write.
          sleep 1
          now=$(current)
          last=$now
          kill "$litellm_pid" 2>/dev/null
          wait "$litellm_pid" 2>/dev/null
          start
        elif ! kill -0 "$litellm_pid" 2>/dev/null; then
          wait "$litellm_pid" 2>/dev/null
          start
        fi
      done
    readiness-probe:
      http-get:
        path: /health/readiness
        port: {{ $litellm_port }}
        scheme: HTTP
      initial-delay-seconds: 10
      period-seconds: 10
      timeout-seconds: 5
      failure-threshold: 30
      success-threshold: 1
    autoscaler:
      metrics:
        enabled: true
        # LiteLLM serves the Prometheus exporter at /metrics/ (the bare path
        # 307-redirects there); scrape the canonical path directly.
        path: /metrics/
        port: {{ $litellm_port }}
        interval: 15
      scale-up:
        triggers:
          # One trigger only: submit-time validation allows a target service to
          # be named by at most one trigger. Scale-policy queries are evaluated
          # against the owning replica's own scraped series, so only
          # LiteLLM-visible signals can appear here: failed requests wake an
          # empty or failing pool, sustained request rate grows it under load.
          # Route filter: metrics scrapes and readiness probes are counted in
          # these counters too and would satisfy the rate threshold on their
          # own; only real API traffic may scale the pool.
          - metrics-query: >-
              ((sum(increase(litellm_proxy_failed_requests_metric_total{route=~"/v1/.*"}[1m])) or vector(0)) > bool 0)
              + ((sum(increase(litellm_proxy_total_requests_metric_total{route=~"/v1/.*"}[1m])) or vector(0)) >= bool {{ $scale_up_rpm }})
            cooldown-period: {{ $scale_up_cooldown }}
            services: [vllm]
  {{- end }}
